import React from "react";
import { Text, StyleSheet } from "react-native";

const HomeScreen = () => {
  return <Text style={styles.text}>Hello Dylan</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 25
  }
});

export default HomeScreen;

